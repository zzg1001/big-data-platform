## "标签管理平台"
### 行级标签
   1.从数据源里选择一个表，展示表结构字段。
   2.选择相关的表字段。并且创建新字段（标签字段可以创建多个），创建新表：带有标签的新表。
   3.然后选择某类标签。
   4.然后将此表的信息和选择的标签给大模型，大模型将读取表数据将标签打在数据上写到行表里。
### 规则标签
 -- 第一种
    1.点开已经打好的标签，选择标签筛选数据：形成数据集，自动创建一个表把数据写进去。
    2.将这个数据集打上标签，当时打标签立马自定义标签名字。

 -- 第二种
    类似“数据开发模块-》数据探索”：写好了逻辑，点击保存为标签-〉定义标签名
### 标签管理页面
    1.标签的生命周期创建
    2.标签的层级关系的定义。
    3.层级类型定义：可以是